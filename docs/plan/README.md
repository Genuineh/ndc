# Plan 文档约定

`docs/plan` 用于维护计划文档，规则如下：

1. `current_plan.md`：当前唯一有效计划（持续更新）。
2. `archive/`：已完成计划归档，不再直接编辑。
3. `docs/TODO.md`：只维护待办项，不放完整计划正文。

## 维护流程

1. 新周期开始：更新 `current_plan.md`。
2. 周期完成：将 `current_plan.md` 归档到 `archive/`（建议保留日期/主题）。
3. 归档后：创建新的 `current_plan.md` 作为下一周期计划。
